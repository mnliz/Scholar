[project]
name = 'scholar'
version = '0.0.1'
description = 'RAG-based knowledge management tool'
requires-python = '>=3.11'

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'

[tool.hatch.build.targets.wheel]
packages = ['src/scholar']